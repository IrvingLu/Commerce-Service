using Autofac;
using Module = Autofac.Module;

namespace Pay.Web.Infrastructure
{
    public class DependencyRegistrar : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
           
        }
    }
}
